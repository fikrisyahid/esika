const BASE_LOADING = {
  duration: 500,
};

const LOGIN_LOADING = {
  duration: 500,
};

const UNAUTHORIZED_LOADING = {
  duration: 2000,
};

export { BASE_LOADING, LOGIN_LOADING, UNAUTHORIZED_LOADING };
